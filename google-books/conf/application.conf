# conf/application.conf

google.auth {
  clientId = "432449136597-b79cre253mc9pcpfjopv0mr5r22m9mlq.apps.googleusercontent.com" // Read from env var
  clientSecret = "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" // Read from env var
  // IMPORTANT: This redirect URI MUST EXACTLY match one listed in your Google Cloud Console credentials
  // Even though we use popup flow, Google often requires it for validation during code exchange.
  // Use the one *without* the /callback part for the code exchange itself usually.
  // Check Google's docs if you face issues, sometimes it needs to be exactly 'postmessage' for popup flow.
  redirectUri = ${?GOOGLE_REDIRECT_URI}"http://localhost:3000" // Default for local dev frontend
  tokenUri = "https://oauth2.googleapis.com/token"
  // Optional: Google's public key URL for stricter ID token validation
  // jwksUri = "https://www.googleapis.com/oauth2/v3/certs"
}

google.books {
  url = "https://www.googleapis.com/books/v1/volumes"
  apiKey = "AIzaSyBmG8l0eOM-PFRXtBeLyFs7akGILX_2zPA"
}

play.filters.enabled += "play.filters.cors.CORSFilter"
play.filters.cors {
  allowedOrigins = ["http://localhost:3000"]
  allowedHttpMethods = ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
  allowedHttpHeaders = ["Accept", "Content-Type", "Authorization"]
  supportsCredentials = false
}
